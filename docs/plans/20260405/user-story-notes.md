### Mga tasks na dapat gawan ng issue on github

- [x] [TRANSACTION_HISTORY_PLAN](./TRANSACTION_HISTORY_PLAN.md) the data in recent transactions in the dashboard and the data in transaction logs in logs is not consistent, ano ang dapat natin sundin?

- [x] [DYNAMIC_INFO_CARD_PLAN](./DYNAMIC_INFO_CARD_PLAN.md) Auto-generated Info Card in @fragment_borrow_item.xml should also exist in @fragment_return_item.xml with same position as in the @fragment_borrow_item.xml. Also, the information in that Info Card is not dynamic, the date and time should be alive and going in the "Date & Time:" field, the "Processed by" field is should not be hardcoded with "System Staff" and should be the current user's full name, and the "Return by:" should be remove because i think this is useless.

- [x] [INVENTORY_LAYOUT_REFINE_PLAN](./INVENTORY_LAYOUT_REFINE_PLAN.md) the "Add Item" button in @fragment_inventory.xml is unpleasant to see beside the inventory title, find a way to relocate or relayout this button. I think this button is better to see below the title and above the search bar (between) just like "Add User" button in the @fragment_user_management.xml.

- [x] [LOG_FILTER_REFINEMENT_PLAN](./LOG_FILTER_REFINEMENT_PLAN.md) the actions filter in @fragment_transactions_logs.xml shouldn't be "Items Borrowed" & "Items Returned". Thus, should be "Borrowed" & "Returned". This issue exists also exists in @fragment_activity_logs.xml the actions filter should be the universal actions (created, updated, & deleted) this should cater all system operations dynamically in @fragment_activity_logs.xml except for @fragment_transactions_logs.xml because it has a dedicated actions filter of "Borrowed" & "Returned" (i think this a backend and frontend fix).

- [x] [AUDIT_LOG_INTEGRITY_PLAN](./AUDIT_LOG_INTEGRITY_PLAN.md) the client notice that the data of activity_logs table in regards to the snapshot of every entry might be misleading at not consistent for finding the source of truth when it comes to the app user who performed the borrow, return, or the activity. This is because the activity_logs table depends on 'performed_by' and 'target_user_name' fields/columns, this can be misleading because the system user can update their names and student names. So we shall remove this two misleading table columns in activity_logs and to always lookup the name of actor_id and target_user_id for dynamic referencing. im not 100% sure to include the removal of 'target_user_id' compared to 'actor_id'. So, let me know the impact if this is destructable and will not work as expected.

- [x] [RBAC_ENFORCEMENT_PLAN](./RBAC_ENFORCEMENT_PLAN.md) the client notice the unhandled bugs permission or authorization when we login in the app as a staff. user management & student management shouldn't visible in staff. So, hide the user management & student management option in the dropdown menu profile and protect that api endpoints from the staff regards to that, and transaction logs and activity logs must be visible in staff but there is an exception in the data that getting fetch in activity logs for staff. staff only see his/her own account (staff) activity logs data. if the user is admin, the activity logs data that should be fetch is all (this is a frontend and backend fix).

- [~] [LOG_PAGINATION_AND_FILTER_PLAN](./LOG_PAGINATION_AND_FILTER_PLAN.md) the client wants the @fragment_transactions_logs.xml and @fragment_activity_logs.xml to be paginated just like in the @fragment_inventory.xml. Also, client wants date filter period, but not sure how long is the option of date filter period, this date filter period need lacks of concrete plan so discussion for finalization for this date filter period matter is expected.

- [x] [USER_DIALOG_UI_CONSISTENCY_PLAN](./USER_DIALOG_UI_CONSISTENCY_PLAN.md) in @diaglog_user.xml, the user_field_role input field ui is not consistent with the other fields because it lacks a padding or space in the inline-start.

- [~] [USER_SELF_MANAGEMENT_PLAN](./USER_SELF_MANAGEMENT_PLAN.md) in @fragment_user_management.xml, the user (admin) is listed in the user lists including the user itself, so the user needs a sign or indication that he/she is really the user itself in that list and the system should disabled edit & delete btn for the user itself in that list.

- [~] [COURSE_READABILITY_PLAN](./COURSE_READABILITY_PLAN.md) the user notice that the course input fields across the system is not ui consistent in text, the course is not readable it appears as somethign like this when selecting a course: Bachelor in Science in Co..., Bachelor of Arts in Communi..., etc. This is specfically happening in the Borrow Form in @fragment_borrow_item.xml and @fragment_student_management.xml.
